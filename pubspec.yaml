name: flutter_apprun
description: A lightweight, easy-to-use, event-driven state management system, highly inspired by AppRun JS
version: 0.8.0
repository: https://github.com/yysun/flutter_apprun

environment:
  sdk: '>=2.19.1 <3.0.0'

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.22.0
  flutter_test:
    sdk: flutter

dependencies:
  flutter:
    sdk: flutter

